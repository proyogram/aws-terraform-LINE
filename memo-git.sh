# git clone
REPO_NAME=aws-terraform-LINE
BRACH_NAME=develop
git clone https://github.com/proyogram/${REPO_NAME}.git
cd ${REPO_NAME}
git checkout -b ${BRACH_NAME}
git status


# git new branch
REPO_NAME=aws-terraform-LINE
BRACH_NAME=develop
cd ${REPO_NAME}
git checkout -b ${BRACH_NAME}
git status



